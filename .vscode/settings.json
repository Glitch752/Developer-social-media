{
    "files.exclude": {
        // "**/*.js": { "when": "$(basename).ts" },
        // "**/**.js": { "when": "$(basename).tsx" },
        "**/**.js.map": true,
        "package.json": true,
        // "node_modules": true,
        "package-lock.json": true,
        "**.code-workspace": true
    }
}